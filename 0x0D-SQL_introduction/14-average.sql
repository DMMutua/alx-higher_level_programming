-- Computing the score average of all records in `second_table`
-- Database: `hbtn_0c_0` [passed as argument to the mysql command]
-- Result column name is `average`
SELECT AVG(score) AS average FROM second_table;
