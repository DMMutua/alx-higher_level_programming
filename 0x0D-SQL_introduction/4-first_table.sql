-- Creating a table called `first_table` in the
-- current database of MySQL server.
--	If the table already exists, the script does not raise errors.
CREATE TABLE IF NOT EXISTS first_table (
	id INT,
	name VARCHAR(256)
	);
