-- Listing all records of the table `second_table`
-- of the database `hbtn_0c_0` in MySQL server.
-- Lists them from the best score to worst score

SELECT score, name FROM second_table ORDER BY score DESC;
